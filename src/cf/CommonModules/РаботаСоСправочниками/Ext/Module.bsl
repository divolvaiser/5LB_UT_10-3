
Процедура ПриЗаписиНоменклатурыПриЗаписи(Источник, Отказ) Экспорт
	Если (Не РольДоступна(Метаданные.Роли.ПолныеПрава)) И (Не РольДоступна(Метаданные.Роли.IDDQD)) Тогда
		Если (Источник.ЭтоГруппа) И (ЗначениеЗаполнено(Источник.Родитель))  Тогда
			Сообщить ("Нельзя перемещать группы!",СтатусСообщения.Важное);
			Отказ=Истина;
		КонецЕсли;
	КонецЕсли;
		
КонецПроцедуры
